# General Preferences
- Prefers Next.js 15 (App Router) + Tailwind CSS v4 + Framer Motion + TypeScript as a default frontend stack. Confidence: 0.9
- Uses Google Fonts (Inter) with system font fallbacks. Confidence: 0.8
- Dark mode only — explicitly rejects light mode and light mode toggles. Confidence: 0.95
- Accent/signal colors should be used sparingly (e.g. hover states, highlights, hyperlinks) — never as button fills or backgrounds. Confidence: 0.9
- Prefers barely-visible decorative UI elements (e.g. hairline grid overlays at ~1.8% large-grid / ~0.8% small-grid opacity) for subtle texture — "a whisper not a statement." Confidence: 0.85
- CTA links should be plain text with icon prefixes, not pill buttons or heavy UI components. Confidence: 0.9
- Uses CSS `clamp()` for fluid responsive typography with specific min/max bounds. Confidence: 0.85
- Prefers editorial 12-column CSS Grid layouts for hero/landing sections. Confidence: 0.8
- Applies tight negative letter-spacing on large display text (e.g. -5.5px). Confidence: 0.8
- Uses Framer Motion with staggered entrance animations (fade-in, slide-up, scale-pop). Confidence: 0.85
- Prefers custom cubic-bezier easing: `[0.16, 1, 0.3, 1]`. Confidence: 0.8
- Prefers clean component separation — each logical UI section gets its own file (e.g. HeroSection.tsx, CTALinks.tsx, SocialLinks.tsx). Confidence: 0.9
- Prefers CSS media queries over JavaScript window checks (`window.innerWidth`) for responsive layout behavior. Confidence: 0.9
- CSS class naming convention: feature-prefixed semantic names (e.g. `project-overlay-inner`, `project-overlay-close`, `project-overlay-title`). Confidence: 0.85
- Dual styling strategy: CSS classes handle responsive layout/behavior (grid-to-flex, padding, sizing), inline styles handle base visual properties (colors, fonts, borders). Confidence: 0.85
- Uses `!important` in CSS media query overrides when responsive adjustments must win over inline styles. Confidence: 0.8
- Uses `"use client"` directive for interactive components in Next.js App Router. Confidence: 0.8
- Uses conventional-commit-style prefixes: `init:`, `feat:`, `fix:`, `refactor:`, `revert:`. Confidence: 0.9
- Explicitly does NOT want co-author trailers (`Co-Authored-By`) on commits. Confidence: 0.95
- Structures commits in logical stages: scaffold → feature → responsive/polish. Confidence: 0.8
- Prefers non-destructive git history: when undoing commits, use inverse `git revert` commits instead of rewriting history (`git reset --hard`), keeping prior commits intact. Confidence: 0.85
- Expects a build verification (`npm run build`) after each logical change before committing, to catch regressions early. Confidence: 0.85
- Explicitly bans em dashes (—) from all code and content. Confidence: 0.95
- Prefers non-destructive additions — new sections/components should not modify existing files. Confidence: 0.9
- Uses numbered section labels (e.g. "02 — About") for page sections to indicate hierarchy. Confidence: 0.8
- Prefers horizontal swipeable carousel with scroll-snap for mobile card layouts (85vw card width, ~1.15 cards visible to hint at scrollability), with IntersectionObserver-powered dot indicators. Confidence: 0.85
- Prefers fixed right-side scroll dot navigation for single-page portfolios: 6px dots, #333333 default / #ffffff active / #999999 hover, with absolutely-positioned uppercase labels (11px, 0.12em letter-spacing) fading in on hover, IntersectionObserver at 50% threshold, staggered Framer Motion mount animation, hidden below 768px. Confidence: 0.85
- Wants commented-out `<video>` elements ready to uncomment alongside placeholder divs (e.g. showing initials with pulsing animation) so real media can be swapped in later. Confidence: 0.85
- Prefers card hover states that expand height with smooth cubic-bezier transitions, rotating arrow icons, and accent-colored border reveals. Confidence: 0.8
- Prefers horizontal grid-column stretch on hover: dynamically adjusting `grid-template-columns` ratios (e.g. `1.4fr 0.6fr`) so the hovered card grows wider while the sibling shrinks, with `0.5s cubic-bezier(0.16, 1, 0.3, 1)` transition. Confidence: 0.85
- Prefers row-based grid architecture for independent hover states: each row of cards wrapped in its own grid container with its own `useState` for hover tracking, rather than a single grid with global state. Confidence: 0.85
- Uses Framer Motion `layoutId` + `AnimatePresence` for smooth card-to-fullscreen-overlay expand/collapse transitions. Confidence: 0.85
- Consistent pill-style UI elements (status pills, tech chips, action buttons) with backdrop-blur, semi-transparent backgrounds (e.g. `bg-white/10`), and 0.5px hairline borders. Confidence: 0.85
- Section padding convention: 96px top/bottom and 60px left/right on desktop, reduced to 16px/24px on mobile via responsive classes. Confidence: 0.8
- Canonical mobile breakpoint is `max-width: 767px` (strictly below 768px) for CSS media queries, and mobile-only overrides must be scoped inside that query so desktop styling stays untouched. Confidence: 0.75
- Communicates debugging tasks as numbered, step-by-step checklists with specific hypotheses and explicit file-scope constraints (e.g. "Do not modify X or Y"). Confidence: 0.9
- Provides full implementation specs with exact CSS values, component architecture, state management approach, and even the commit message — essentially a prescriptive design document rather than a high-level request. Confidence: 0.9
- References external visual references — specific websites (e.g. "Porsche website reference") or a supplied reference image (e.g. "matching the reference", "the reference shows...") — for design matching and expects close visual/behavioral fidelity to the named reference, even when the agent cannot see it and must reason about the intended look from the written spec. Confidence: 0.85
- Prefers temporary visual debugging: add colored borders (e.g. `2px solid red`) to confirm elements exist in the DOM before investigating deeper logic issues. Confidence: 0.85
- Debugging methodology: strip away complexity first (e.g. remove AnimatePresence/layoutId, use simple useState toggle) to isolate the root cause, then restore animations cleanly once the base renders correctly. Confidence: 0.85
- Uses @emailjs/browser for client-side contact form submission (no backend). Confidence: 0.85
- API keys and service credentials should be stored as clearly-named placeholder constants (e.g. `YOUR_SERVICE_ID`) at the top of the file for easy find-and-replace. Confidence: 0.9
- Footer layout convention: left (name), center (tech stack credit), right (year), with responsive vertical stacking on mobile. Confidence: 0.8
- Form validation pattern: inline error messages below each field in 12px red (#ff4444), cleared on field change. Confidence: 0.85
- Prefers status pill notifications for form feedback: rounded-full pills with rgba-tinted backgrounds and matching 0.5px borders (e.g. green rgba for success, red rgba for error), animated with Framer Motion fade-in. Confidence: 0.85
- Submit button state machine: idle (default colors) → loading (disabled, "Sending...") → success (green bg, "Message sent") → error (red bg, specific error text), with auto-reset after timeout. Confidence: 0.85
- Social links pattern: horizontal row with hairline dot separators (·), uppercase, letter-spaced, muted color (#555555) that brightens to white on hover. Confidence: 0.8
- Uses `uisfx` library for UI sound effects with a centralized singleton player (`lib/sfx.ts`), SSR guards (`typeof window`), lazy initialization, and localStorage persistence for enabled/volume preferences. Confidence: 0.9
- Audio unlock pattern: one-time `pointerdown`/`keydown` listener in a dedicated `SFXUnlocker` component placed in the root layout, gating all `playSFX` calls until first user gesture. Confidence: 0.9
- When rules forbid modifying a component file, creates a wrapper component using event delegation (e.g. `HeroSectionSFX` wrapping `HeroSection` with `onPointerOver`/`onClick` handlers) rather than violating the constraint. Confidence: 0.85
- Sound and motion are independent accessibility settings -- `prefers-reduced-motion` does NOT suppress audio. Confidence: 0.9
- Sound reinforces but never replaces visual state changes, ARIA updates, form validation messages, or overlay animations. Confidence: 0.9
- Selective sonification: only intentional, sparse interactions get sound (clicks, focus, opens/closes, navigation). Does NOT sonify scrolling, passive layout changes, or hover states on dense repeated elements. Confidence: 0.9
- Documents action-to-cue mapping as a comment block at the top of the SFX module file. Confidence: 0.85
- Sound preference toggle (SFXToggle) placed in the contact/footer section below social links, with `aria-label` describing current state and visible text label. Confidence: 0.85
- Uses Aceternity UI as a component library for animated/interactive UI primitives (e.g. dotted glow background). Installs via shadcn CLI or direct registry JSON fetch. Confidence: 0.85
- Prefers canvas-based animated backgrounds (e.g. DottedGlowBackground) over CSS pseudo-element textures for section backgrounds -- canvas gives per-dot shimmer/glow animation. Actively reverted a CSS grid texture overlay in favor of clean dark canvas, reinforcing this preference. Confidence: 0.9
- Performance-tiered canvas animations: interactive sections (Specializations, Contact) get full animation speed; passive/scroll-heavy sections (Projects, About, Footer) get reduced speed (speedMin 0.1, speedMax 0.4) to reduce GPU load. Confidence: 0.85
- Requires a z-index audit after adding absolutely-positioned background layers -- all section content must be verified at z-index 1+ to render above z-index 0 backgrounds. Confidence: 0.9
- Multi-commit strategy for component swaps: (1) install new component, (2) remove old component + cleanup, (3) apply new component to sections. Each commit is atomic and buildable. Confidence: 0.9
- Uses CSS radial gradient mask utilities for directional glow concentration on section backgrounds -- each section gets a unique mask direction (top-right, bottom-left, center, etc.) matching its visual energy direction. Confidence: 0.85
- Section background opacity and speed are tuned per-section based on importance: hero-adjacent sections get higher opacity (0.65), footer gets lowest (0.35). Confidence: 0.8
- Extracts tunable values into a top-of-file config object (e.g. `MEDIA_CONFIG`) with a detailed HOW TO USE comment block, so non-technical users can edit images, scales, and positions without touching component logic. Confidence: 0.85
- Prefers Lenis (`@studio-freight/lenis`) for smooth scroll, with named constants at the top of the provider component for easy tuning (e.g. `LENIS_DURATION`, `LENIS_TOUCH_MULTIPLIER`). Confidence: 0.9
- When using Lenis with Framer Motion, re-dispatches `scroll` events via `lenis.on("scroll", () => window.dispatchEvent(new Event("scroll")))` so `useScroll` hooks continue working. Confidence: 0.9
- Prefers React Three Fiber with conservative Canvas settings: `antialias: false`, `alpha: true`, `powerPreference: "low-power"`, and `pointerEvents: "none"` on both wrapper and Canvas to avoid blocking interactions. Confidence: 0.9
- Framer Motion convention: always provide a matching `initial` prop for every animated SVG/CSS property (strokeOpacity, opacity, fill, etc.) — use the falsy/default value as the initial. Prevents console warnings and ensures clean first-render state. Confidence: 0.9
- Responsive performance tuning uses JavaScript viewport checks (e.g. `window.innerWidth < 768`) for resource count adjustments (particles, objects), not CSS media queries — because these affect runtime compute, not layout. The same JS check (with a `resize` listener and an initial run so SSR/first render match) is also expected for mobile-specific *interaction* logic that CSS cannot express, such as tap-vs-hover. Confidence: 0.85
- Prefers `next/dynamic` with `ssr: false` wrapped in a dedicated `"use client"` wrapper component (e.g. `ClientParticleField`) when a Server Component layout needs client-only libraries like Three.js. Confidence: 0.9
- Prefers background canvas/WebGL layers at `z-index: 0` (fixed, full-viewport, `pointerEvents: "none"`) with all content sections verified at `z-index: 1+`. Confidence: 0.9
- Mobile overlay back-button pattern: uses History API (`pushState`/`popstate`) to intercept Android hardware back button and iOS swipe-back gesture, closing overlays instead of navigating away from the page. `closeOverlay()` calls `history.back()` when cleaning up via X/Escape. Confidence: 0.9
- Overlays that must break out of parent stacking contexts (e.g. `transform`, `sticky` + `z-index`) should use React `createPortal` to render into `document.body`, bypassing all ancestor stacking contexts. Confidence: 0.9
- Overlay z-index convention: 9999 for primary overlays to guarantee they beat all other stacking contexts in the app. Confidence: 0.85
- When a deprecation warning originates from a third-party dependency (not user code), suppress it with a targeted `console.warn` override (filtering by message substring) at module level rather than attempting to fix the dependency's internals. Confidence: 0.85
- Overlay content panels must have `overflow: hidden` on the container and `overflow: auto` on scrollable inner regions to prevent media (images, video) from visually escaping their bounds. Confidence: 0.85
- Mobile touch target specs for interactive rows: `min-height: 60px`, `touch-action: manipulation` (kills 300ms tap delay), `-webkit-tap-highlight-color: transparent`. Confidence: 0.85
- When using a custom cursor overlay (e.g. custom blue arrow), enforces `cursor: "none"` on all interactive containers and replaces every instance of `cursor: "pointer"`, `cursor: "default"`, `cursor: "auto"` in inline styles with `cursor: "none"` — no exceptions. Media/expand blocks within hover rows get `pointerEvents: "none"` to avoid stealing hover events from parent rows. In CSS, also adds explicit element-specific overrides (`a`, `button`, `[role="button"]`) alongside the `*` rule because some browsers apply UA stylesheets that beat the `*` selector. Confidence: 0.9
- Mobile overlay content panels must be scrollable: `overflowY: auto`, `-webkit-overflow-scrolling: touch`, `maxHeight: 100%` on the overlay's content container. Confidence: 0.85
- Lenis scroll lock during overlays: must call `lenis.stop()` (or use the project's `lockScroll()` equivalent) when an overlay opens and `lenis.start()` / `unlockScroll()` when it closes, to prevent Lenis from re-enabling scroll despite `body overflow: hidden`. Confidence: 0.9
- Inline expand/collapse blocks must never gain an internal scrollbar: avoid `overflow: auto`/`overflow: scroll` inside them and instead clip long text with `-webkit-line-clamp` + `overflow: hidden` (distinct from full-screen overlays, whose inner regions are intentionally scrollable). Confidence: 0.8
- Compact expand/detail panels follow a left-media / right-text two-column structure: small uppercase status or category label, a 2-3 line clamped description, then a single row of tech chips capped at 4 (`slice(0, 4)`) — without repeating the title already shown in the parent row. Confidence: 0.75
- Expand/detail block body text should render the full `description` (not the smaller muted tagline) with description-paragraph styling: `#cccccc`, `line-height: 1.55`, `-webkit-line-clamp` (3 to 4 lines). The exact font size is not stable (specs have asked for 15px and then 13px), so follow the current reference rather than a remembered value. Confidence: 0.55
- Uses prior commits/versions as the concrete reference for a change (e.g. "make text body similar to latest commit", "revert this to three previous versions") and expects the result to match that version closely. Confidence: 0.75
- Leaves working-tree changes uncommitted unless a commit is explicitly requested; does not assume every edit should be committed. Confidence: 0.7
- When the user says "push" (or "push everything"), they mean literally every uncommitted change — including auto-managed/tooling files such as the `.commandcode/taste/.../taste.md` learning file — should be committed and pushed, not left behind as a local modification. Expects the working tree to end fully clean and in sync with the remote. Confidence: 0.75
- Custom cursor is implemented as a single self-contained component with no abstraction: a `requestAnimationFrame` loop writes `transform: translate(x, y)` directly to the DOM node through a ref (`position: fixed; top: 0; left: 0`, `willChange: transform`, `pointerEvents: none`, high z-index), while mouse coordinates live in a ref with zero React state updates — chosen explicitly for "zero lag" tracking. Confidence: 0.85
- Custom cursor visibility is opacity-driven rather than abrupt, and is driven by a single boolean `active` state toggled directly by events — `mousemove` sets it true, `scroll` sets it false immediately with no timeout/debounce or settle timer. The fade is asymmetric/directional based on hidden state: slower out (`opacity 0.12s ease-out`) and faster in (`opacity 0.08s ease-in`) so reappearance feels nearly instant. Disabled entirely below 768px via a JS viewport check that returns `null` (no touch/mouse to track). Confidence: 0.8
- Prefers simple, event-driven boolean state over timer-based/deduped state machines: explicitly asks to delete scroll timers and `clearTimeout` cleanup and replace them with a single boolean flipped immediately by the relevant event listener. Confidence: 0.7
- Cursor is mounted at the body root (first child of `<body>`, outside any provider scope) so the fixed-position overlay works over portal-rendered content as well. Confidence: 0.8
- Prefers replacing a complex provider/context-based implementation with a simpler, more reliable one — explicitly asks for a "rebuild from scratch" rather than patching the existing code, and expects the old provider/wrapper components and their usages to be removed rather than left as dead code. Confidence: 0.8
- Before deleting a file as dead code, greps the source tree for remaining imports (`grep -rn "from.*cursor" src/`) and only deletes once nothing references it. Confidence: 0.85
- Prefers strictly minimal, surgical edits: for a small fix, specifies the exact find/change snippet, calls it "the only change," and insists nothing else in that file — or any other file — be modified. Confidence: 0.8
- Maintains a strict design palette: any newly introduced color must be a close complement/derivative of the existing accent rather than an unrelated hue (e.g. a gradient from the exact accent blue `#0099ff` to a deep blue-purple `#6644ff` chosen to "complement without introducing a new unrelated color"). Decorative glows reuse the accent blue's rgba (e.g. `rgba(0,153,255,...)`) to match every other glow on the site. Confidence: 0.8
- When replacing a visual asset (SVG shape, icon), expects the whole element to be swapped for the provided block verbatim while leaving surrounding behavior (fade logic, scroll behavior, responsive/mobile hiding) untouched. Confidence: 0.7
- Redesigns should restyle within the project's existing design system rather than inventing new values: reuse the token vocabulary already used elsewhere (Tailwind utilities backed by the `@theme` variables in globals.css, e.g. `bg-surface-1`, `bg-surface-2`, `border-hairline`, `text-ink-muted`, `text-ink-faint`, `text-accent-blue`) instead of fresh hardcoded hex colors or one-off inline palettes. Confidence: 0.7
- Pushes back when a "redesign" request would delete an existing interaction pattern (e.g. the mobile horizontal swipe carousel): wants the working behavior preserved and only the visual treatment changed, and expects a change that already landed to be undone rather than kept. Prefers a stateful, swipeable carousel over collapsing it into a tap-to-expand vertical list on mobile. Confidence: 0.65
- Mobile parity pattern for hover-driven UI: touch devices get a parallel tap state (e.g. `tappedSegment`) that is unified with the hover state into one derived "active" value (`activeSegment = isMobile ? tapped : hovered`), and every hover check in the component is rewritten to read that single active value. Tapping the same item again collapses it, and tapping another item expands it while collapsing the previous one. Hover handlers are explicitly disabled on mobile (`isMobile ? undefined : handler`). Confidence: 0.7
- Everything that reacts to the active/hovered item — video play/pause, opacity/dim animations, clip paths — must be re-keyed off the unified active value, not left on the raw hover state, so mobile behaves identically to desktop. Media playback follows the active item only: the active item's clip plays and every other clip is paused and rewound to 0. Confidence: 0.7
- Mobile-specific changes must be gated so desktop behavior is untouched: the spec explicitly forbids breaking desktop hover, and requires the active state to resolve to the hovered segment on desktop and the tapped segment on mobile. Confidence: 0.7
- Touch flow for expandable media segments: the first tap only expands the segment and starts its media; it must never open the full-screen overlay directly. A small "View details" pill (dark translucent, backdrop-blur, hairline accent-tinted border, pill radius, plus icon) appears inside the expanded area and is the only thing that opens the project overlay. When the overlay closes, the tapped segment collapses too. Confidence: 0.65
- Interactive HTML that gets cut off by an SVG `clipPath`/`foreignObject` should be re-parented into its own absolutely-positioned HTML overlay layer stacked above the SVG (`inset: 0` on the layer, `pointerEvents: "none"` on the layer and `all` plus a higher `zIndex` on each item) inside the same unclipped container, instead of trying to work around the clip inside the SVG subtree. Anchors for such overlay items are computed in the container's own coordinate space (e.g. radius + angle from the segment's mid-angle) and ride the container's existing scale without extra conversion. Confidence: 0.7
- When a spec's literal content values conflict with the surrounding content or the labels already in the file, prefers the spec applied verbatim: given the choice between renaming only what was inconsistent and applying the provided list exactly, chose the exact list even though titles then no longer matched their own descriptions and demo clips. Confidence: 0.6
- Treats background treatment by scope: per-section backgrounds are canvas-based animated layers, but a static, faintly-visible repeating texture (e.g. a diamond quilt: two 45/-45 degree gradient line sets over the #090909 canvas) belongs in the global CSS on body/section/footer so it reads across the whole site, and expects opaque or overriding section backgrounds to be dealt with so the texture is not hidden. Treat the concrete texture numbers as a moving target that keeps being ratcheted upward across passes (0.018 alpha / 18px tile, then 0.06 alpha / 22px, then 0.12 alpha with 1.5px lines / 24px tile, then the whole tile rebuilt as embossed filled diamonds: four hard-stop 25% corner gradients at white 0.07 plus a 135deg white 0.04 highlight wash and a 315deg black 0.15 shadow wash on a charcoal base a step lighter than #090909) - follow the values in the current request rather than a remembered set. Expect the texture technique itself (line lattice vs filled emboss) to be swapped wholesale rather than tuned, and an attempt that does not match the reference to be reverted in full rather than salvaged. Confidence: 0.65
- Wants decorative background textures to actually register on screen: if a subtle texture renders essentially imperceptible, expects its values tuned up (line opacity, then tile size) rather than left invisible -- while still judging the result against a "visible but not overpowering, layout/content still reads cleanly" bar. The same bar applies to the canvas dot layer: intensity gets bumped via `opacity={1}`, a larger dot `radius`, a tighter `gap`, and a brighter glow color token (sky-700 -> sky-600). Confidence: 0.6
- Expects decorative animation to be gated behind `@media (prefers-reduced-motion: reduce)` with `animation: none !important` on the same selector list (and, once the animation drives a property such as `background-size`, that property pinned back to its resting value with `!important` too), treated as an accessibility opt-out independent of the sound setting. Confidence: 0.8
- Ambient/decorative motion must be very slow and barely noticeable (e.g. a 12s `ease-in-out infinite` texture drift, an 8s zoom pulse, then a 6s/10s shimmer sweep) rather than anything attention-grabbing, judged against a "noticeable but not distracting" bar for the amount of movement. Concrete durations and value ranges are a moving target, and the animation technique itself gets swapped wholesale every round (background-position drift -> background-size zoom pulse breathing 24px -> 32px -> moving pseudo-element highlight gradient sweeps): follow the values in the current request rather than a remembered set, and expect the whole mechanism to be replaced rather than tuned. Confidence: 0.8
- Staggers the same looping ambient animation per section using negative `animation-delay` values (-2s, -4s, -6s, -8s, -10s, later -1.5s steps ending at -7s with an explicit `body { animation-delay: 0s }`) so the animation is already running and mid-cycle on page load with no delay before first movement, each section out of phase with its neighbors to create a ripple/depth effect instead of everything moving as one. Exact delay values change per request. Confidence: 0.8
- Looping decorative animations must be seamless: the animated property travels exactly one full tile/period and returns to its start (e.g. `background-position: 0 0` -> `24px 24px` -> `0 0` against a 24px background tile) so the loop point has no visible jump. Confidence: 0.65
- Expects the animation targets to be tied to the existing texture rule and section selectors already in globals.css (all section IDs resolve to `<section>` elements), reusing those selectors rather than inventing new ones. Confidence: 0.6
- Wants opaque section backgrounds made semi-transparent so a decorative layer sitting behind the sections shows through, changing `background-color` on the existing `body, section, footer` rule and keeping the `!important` so it still beats overriding/inline section backgrounds (e.g. `#projects`). The alpha is a moving target retuned against an "enhance depth without overpowering text readability" bar - seen at `rgba(9, 9, 9, 0.82)` (~18% bleed), then `rgba(9, 9, 9, 0.72)` (~28% bleed, described as subtle iridescent depth) - so follow the value given in the current request rather than a remembered one. Confidence: 0.7
- New UI components live in `src/components/ui/` and are imported via the `@/components/ui/...` alias (e.g. `AuroraBackground`, `CustomCursor`), not flat in the components folder. Confidence: 0.7
- Decorative full-viewport canvas layers use `position: fixed; inset: 0; z-index: 0; pointer-events: none` and are marked `aria-hidden="true"` so they stay out of the interaction layer and the accessibility tree, and must never cover text or controls. Confidence: 0.7
- Ambient canvas effects are built from several large soft radial-gradient blobs (no hard edges, very low alpha) that drift on sin/cos oscillation with a slowly pulsing radius at an extremely small per-frame time step, drawn in the site's blue/purple accent palette and layered for depth. Confidence: 0.6
- Prefers raw WebGL with hand-written GLSL (no Three.js / React Three Fiber) for the global site background: a single fullscreen triangle-strip quad, `u_time` / `u_resolution` uniforms driven by one `requestAnimationFrame` loop with a clean `cancelAnimationFrame` + resize-listener teardown, and a simplex-noise (`snoise`) flow field blended through `smoothstep` into the dark/blue/purple palette that matches the rest of the site. Performance is explicitly reasoned about: devicePixelRatio capped at 1.5 to avoid rendering at 2x/3x on retina, and a context created as `{ antialias: false, alpha: false, depth: false, stencil: false }` to cut compositing and memory cost. Confidence: 0.65
- Under `prefers-reduced-motion: reduce` an animated decorative canvas is hidden outright (`display: none !important`) rather than merely paused, with the static texture remaining visible as the fallback look. Confidence: 0.65
- Whether a global decorative background layer should composite under a section that paints its own backdrop or be excluded from it is a moving target, not a settled rule. Earlier it was compositing (the aurora canvas intentionally left to layer under the hero's WebGL shader, "the two layer together") with no hero exception; later it flipped to exclusion, asking for the site-wide dotted glow to be "remove[d] ... from the background part of hero section" so the hero shows only its own shader. The very next request flipped it back yet again, asking for the hero to have "the same bg" as the rest of the site only "50% less brighter", so the dotted glow is read through the hero again at half strength. Follow the current request rather than a remembered stance. Confidence: 0.65
- When a section is carved out of a global background treatment, expects the exclusion to survive the section's own animated/stacked container (e.g. the hero's ~150vh sticky wrapper, which scales down into a card and drops opacity mid-scroll and would otherwise let the layer back in), not just the section element itself. Confidence: 0.5
- States visual intensity adjustments relative to the current rendered effect rather than as absolute values ("make it 50% less brighter", "the same bg but half"), and expects the implementation to land on that exact measured ratio (peak/mean pixel contribution halved, verified against a full-strength reference) instead of a rough eyeballed dimming. Confidence: 0.55
- Global ambient/overlay layers are mounted in the root layout as direct children of `<body>` (e.g. aurora canvas first, then the custom cursor), not inside individual sections. Confidence: 0.65
- Prefers a single site-wide background component (e.g. `GlobalBackground`) mounted once in the root layout as the first child of `<body>`, replacing per-section/per-component background treatments; that wrapper owns the fixed `z-index: 0` / `pointer-events: none` / `aria-hidden` framing, the design-token color mapping, and the mobile performance tier. Confidence: 0.7
- Prefers wrapping installed/registry components (e.g. Aceternity/shadcn primitives such as `DottedGlowBackground`) in a site-local wrapper rather than editing the installed file, leaving the installed component pristine. Confidence: 0.7
- Decorative background layers must never cover content and should not paint their own background (`backgroundOpacity: 0`), so the site's own `background-color` stays authoritative. Confidence: 0.65
- Expects the single global background layer to be readable behind every section for the whole page scroll, not just a strip behind the viewport, and reaches for that by making the sections reveal the fixed layer rather than changing the layer from `position: fixed` to full-document sizing: `section`, the section IDs and `footer` get `background-color: transparent !important` plus `background-image: none !important`, with `body` holding the solid base color (`#090909`, `min-height: 100%`) as the fallback. Confidence: 0.6
- Readability over a now-visible background texture is restored with a per-section pseudo-element wash rather than a section background color: `section::before` / `footer::before` with `content: ""`, `inset: 0`, `rgba(9, 9, 9, 0.55)` and `pointer-events: none`. The alpha is an explicit tuning knob stated in the request (0.4 = more texture visible, 0.65 = more readable) - follow the value in the current request rather than a remembered one. Confidence: 0.55
- Sections must outrank the `z-index: 0` background canvas: `section, footer { position: relative; z-index: 1 }` is asserted in globals.css so contents paint above the background without relying on per-section utilities. Confidence: 0.6
- Styles the native scrollbar to match the dark theme: 6px-wide `::-webkit-scrollbar`, `transparent` track, `#1e1e1e` thumb with a 3px radius, brightening to `#2a2a2a` on hover. Confidence: 0.55
